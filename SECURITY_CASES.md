# 🛡️ SECURITY CASES

> **دراسات الحالة الأمنية - نتعلم من كل ثغرة**  
> كل حالة هي درس، وكل حل هو إنجاز

---

## 📚 فهرس الحالات

| # | الخطورة | التاريخ | الحالة | الباحث |
|---|---------|---------|--------|--------|
| [001](#case-001) | 🔴 حرجة | 2025-01-15 | ✅ محلولة | @security_warrior |
| [002](#case-002) | 🟠 عالية | 2025-02-03 | ✅ محلولة | @code_guardian |
| [003](#case-003) | 🟡 متوسطة | 2025-03-12 | 🟢 جاري | @bug_hunter |

---

## 🔴 CASE-001: SQL Injection في نموذج تسجيل الدخول

### 📋 معلومات الحالة

**الخطورة:** 🔴 Critical (10/10 CVSS)  
**التاريخ:** 2025-01-15  
**الباحث:** @security_warrior  
**الحالة:** ✅ محلولة  
**CVE:** CVE-2025-XXXX

---

### 🎯 الوصف

ثغرة SQL Injection في صفحة تسجيل الدخول تسمح بتجاوز المصادقة والوصول لقاعدة البيانات بالكامل.

---

### 🔬 خطوات إعادة الإنتاج

```bash
# الطلب المستخدم
POST /api/login HTTP/1.1
Content-Type: application/json

{
  "username": "admin' OR '1'='1",
  "password": "anything"
}
```

```sql
-- الاستعلام الضعيف
SELECT * FROM users 
WHERE username = 'admin' OR '1'='1' 
AND password = 'anything'
```

**النتيجة:** تسجيل دخول ناجح بدون كلمة مرور صحيحة! ⚠️

---

### 💥 التأثير

- 🚨 الوصول الكامل لقاعدة البيانات
- 👤 سرقة بيانات جميع المستخدمين
- 🔓 تجاوز نظام المصادقة
- 📊 تعديل/حذف البيانات

**التقييم:** خطورة قصوى - يهدد المشروع بالكامل

---

### ✅ الحل

```javascript
// ❌ الكود الضعيف (قبل)
const query = `SELECT * FROM users 
  WHERE username = '${username}' 
  AND password = '${password}'`;

// ✅ الكود الآمن (بعد)
const query = 'SELECT * FROM users WHERE username = ? AND password = ?';
db.execute(query, [username, hashedPassword]);
```

**التغييرات:**
1. استخدام Prepared Statements
2. Hash كلمات المرور (bcrypt)
3. إضافة Rate Limiting
4. تفعيل WAF Rules

---

### 📊 الإحصائيات

- ⏱️ وقت الاكتشاف → الحل: 6 ساعات
- 🎯 الأنظمة المتأثرة: 3 endpoints
- 🔄 التحديثات المطلوبة: v2.1.0
- 💰 المكافأة: $800

---

### 🎓 الدروس المستفادة

✅ لا تثق في أي مدخل من المستخدم  
✅ استخدم ORM/Prepared Statements دائمًا  
✅ راجع الكود بانتظام  
✅ طبّق مبدأ الأمان من البداية

---

## 🟠 CASE-002: XSS في حقل البحث

### 📋 معلومات الحالة

**الخطورة:** 🟠 High (8.5/10 CVSS)  
**التاريخ:** 2025-02-03  
**الباحث:** @code_guardian  
**الحالة:** ✅ محلولة  

---

### 🎯 الوصف

ثغرة Cross-Site Scripting (XSS) في حقل البحث تسمح بحقن JavaScript ضار.

---

### 🔬 خطوات إعادة الإنتاج

```javascript
// Payload المستخدم
<script>alert(document.cookie)</script>

// الطلب
GET /search?q=<script>alert(document.cookie)</script>
```

**النتيجة:** تنفيذ الكود الضار في متصفح المستخدم

---

### 💥 التأثير

- 🍪 سرقة Cookies والجلسات
- 🎣 Phishing Attacks
- 🔓 Keylogging
- 🌐 إعادة توجيه ضارة

---

### ✅ الحل

```javascript
// ❌ الكود الضعيف
function displaySearch(query) {
  document.getElementById('result').innerHTML = query;
}

// ✅ الكود الآمن
function displaySearch(query) {
  const sanitized = DOMPurify.sanitize(query);
  document.getElementById('result').textContent = sanitized;
}
```

**التغييرات:**
1. استخدام DOMPurify للتنظيف
2. استخدام textContent بدلاً من innerHTML
3. تفعيل Content Security Policy (CSP)
4. تطبيق Output Encoding

---

### 📊 الإحصائيات

- ⏱️ وقت الحل: 4 ساعات
- 🎯 الصفحات المتأثرة: 2
- 💰 المكافأة: $400

---

## 🟡 CASE-003: IDOR في API المستخدمين

### 📋 معلومات الحالة

**الخطورة:** 🟡 Medium (6.5/10 CVSS)  
**التاريخ:** 2025-03-12  
**الباحث:** @bug_hunter  
**الحالة:** 🟢 قيد المعالجة  

---

### 🎯 الوصف

ثغرة Insecure Direct Object Reference تسمح بالوصول لبيانات مستخدمين آخرين.

---

### 🔬 خطوات إعادة الإنتاج

```bash
# المستخدم العادي (ID: 123)
GET /api/users/123/profile
# ✅ ينجح

# محاولة الوصول لمستخدم آخر
GET /api/users/456/profile
# ⚠️ ينجح أيضًا! (يجب أن يفشل)
```

---

### 💥 التأثير

- 👤 الوصول لبيانات مستخدمين آخرين
- 📧 تسريب معلومات حساسة
- 🔄 تعديل بيانات الغير

---

### ✅ الحل المقترح

```javascript
// ❌ قبل
app.get('/api/users/:id/profile', (req, res) => {
  const user = db.getUserById(req.params.id);
  res.json(user);
});

// ✅ بعد
app.get('/api/users/:id/profile', auth, (req, res) => {
  const requestedId = req.params.id;
  const currentUserId = req.user.id;
  
  // التحقق من الصلاحية
  if (requestedId !== currentUserId && !req.user.isAdmin) {
    return res.status(403).json({ error: 'Forbidden' });
  }
  
  const user = db.getUserById(requestedId);
  res.json(user);
});
```

---

## 📊 إحصائيات عامة

| المؤشر | القيمة |
|--------|--------|
| إجمالي الثغرات المكتشفة | 3 |
| الثغرات المحلولة | 2 |
| قيد المعالجة | 1 |
| متوسط وقت الحل | 5 ساعات |
| إجمالي المكافآت | $1,200 |

---

## 🎯 كيف تساهم؟

### للباحثين الأمنيين:
1. ابحث عن ثغرات أمنية
2. أبلغ عبر [`SECURITY.md`](SECURITY.md)
3. ساعد في كتابة دراسة الحالة
4. احصل على مكافأة وتقدير

### للمطورين:
1. راجع الحالات المفتوحة
2. قدّم حلول وPatches
3. حسّن الكود الأمني
4. شارك خبرتك مع المجتمع

---

## 📚 مراجع مفيدة

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE List](https://cwe.mitre.org/)
- [CVSS Calculator](https://www.first.org/cvss/calculator/3.1)
- [Security Headers](https://securityheaders.com/)

---

## 🏆 أبطال الأمان

| الباحث | الثغرات المكتشفة | المكافآت |
|--------|------------------|----------|
| @security_warrior | 1 | $800 |
| @code_guardian | 1 | $400 |
| @bug_hunter | 1 | قيد المعالجة |

---

## 📬 التواصل

**للإبلاغ عن ثغرة:** security@genesislink.io  
**PGP Key:** `0xABCD1234`  
**للاستفسارات:** [GitHub Discussions](https://github.com/genesislink/discussions)

---

<div align="center">

**🛡️ الأمان رحلة، ليس وجهة 🛡️**

[![Report Vulnerability](https://img.shields.io/badge/Report-Vulnerability-critical.svg)](SECURITY.md)
[![Hall of Fame](https://img.shields.io/badge/Hall%20of-Fame-gold.svg)](CREDITS.md)

</div>
